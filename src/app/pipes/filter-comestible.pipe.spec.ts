import { FilterComestiblePipe } from './filter-comestible.pipe';

describe('FilterComestiblePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterComestiblePipe();
    expect(pipe).toBeTruthy();
  });
});
