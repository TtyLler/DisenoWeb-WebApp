import { FilterCajaPipe } from './filter-caja.pipe';

describe('FilterCajaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCajaPipe();
    expect(pipe).toBeTruthy();
  });
});
