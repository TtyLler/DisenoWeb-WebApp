import { FilterEspecialidadPipe } from './filter-especialidad.pipe';

describe('FilterEspecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEspecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
