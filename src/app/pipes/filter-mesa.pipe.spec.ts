import { FilterMesaPipe } from './filter-mesa.pipe';

describe('FilterMesaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMesaPipe();
    expect(pipe).toBeTruthy();
  });
});
