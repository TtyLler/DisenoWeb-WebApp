import { FilterFacturaPipe } from './filter-factura.pipe';

describe('FilterFacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
