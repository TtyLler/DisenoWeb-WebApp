import { FilterBitacoraPipe } from './filter-bitacora.pipe';

describe('FilterBitacoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBitacoraPipe();
    expect(pipe).toBeTruthy();
  });
});
