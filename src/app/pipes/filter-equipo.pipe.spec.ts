import { FilterEquipoPipe } from './filter-equipo.pipe';

describe('FilterEquipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquipoPipe();
    expect(pipe).toBeTruthy();
  });
});
