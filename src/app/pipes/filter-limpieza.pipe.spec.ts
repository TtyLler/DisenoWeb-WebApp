import { FilterLimpiezaPipe } from './filter-limpieza.pipe';

describe('FilterLimpiezaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLimpiezaPipe();
    expect(pipe).toBeTruthy();
  });
});
