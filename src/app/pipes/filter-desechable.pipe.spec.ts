import { FilterDesechablePipe } from './filter-desechable.pipe';

describe('FilterDesechablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDesechablePipe();
    expect(pipe).toBeTruthy();
  });
});
