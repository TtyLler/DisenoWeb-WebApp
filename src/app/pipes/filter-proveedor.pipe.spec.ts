import { FilterProveedorPipe } from './filter-proveedor.pipe';

describe('FilterProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
