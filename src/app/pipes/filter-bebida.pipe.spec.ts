import { FilterBebidaPipe } from './filter-bebida.pipe';

describe('FilterBebidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBebidaPipe();
    expect(pipe).toBeTruthy();
  });
});
